<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.android.myvirtualnutritionist">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyVirtualNutritionist">
        <activity android:name=".MainActivity"></activity>
        <activity
            android:name=".ui.diary.nutrition.NutritionMainActivity"
            android:label="@string/title_activity_nutrition_main"
            android:parentActivityName=".FirstActivity"
            android:theme="@style/Theme.MyVirtualNutritionist.NoActionBar" />
        <activity
            android:name=".FirstActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>